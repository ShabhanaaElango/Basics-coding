//Even or Odd in c
#include <stdio.h>

int main() {
    int n;
    scanf("%d",&n);
    if(n%2==0)
    {
        printf("%d is a Even",n);
    }
    else{
    printf("%d is a Odd",n);
    }

    return 0;
}
